{
  "name": "signal-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npx -y wrangler deploy",
    "start": "npx -y wrangler dev",
    "lint": "npx -y prettier -w ."
  }
}
